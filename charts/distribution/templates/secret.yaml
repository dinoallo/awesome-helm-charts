{{- if .Values.secrets.htpasswd }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "distribution.fullname" . }}-auth
  labels:
    {{- include "distribution.labels" . | nindent 4 }}
type: Opaque
data:
  htpasswd: {{ .Values.secrets.htpasswd | b64enc }}
{{- end }}
